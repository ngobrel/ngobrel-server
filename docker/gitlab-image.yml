version: '3.2'
services:
  build-server:
    volumes:
      - ./:/srv
    build:
      context: ..
      dockerfile: docker/tasks/build-server/Dockerfile
  ngobrel-server:
    image: ${PROJECTIMAGE}:latest
    build:
      context: ..
      dockerfile: docker/tasks/ngobrel-server/Dockerfile
